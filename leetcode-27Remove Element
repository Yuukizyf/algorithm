class Solution {
public:
    int removeElement(vector<int>& nums, int val) {
        int start = 0;
        int n = nums.size();
        for(int i=0;i<n;i++){
            if(val!=nums[i]){
                nums[start]=nums[i];
                start++;
            }
        }
        return start;
    }
};
